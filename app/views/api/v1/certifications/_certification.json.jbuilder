json.extract! certification,
  :id,
  :worker_id,
  :permit_type_id,
  :issued_at,
  :expires_at,
  :document_number,
  :protocol_number,
  :protocol_date,
  :training_center,
  :next_check_date,
  # 🚅 super scaffolding will insert new fields above this line.
  :created_at,
  :updated_at

# 🚅 super scaffolding will insert file-related logic above this line.
