<%= render 'account/shared/page' do |page| %>
  <% page.title t('.section') %>
  <% page.body do %>
    <%= cable_ready_updates_for @certification do %>
      <%= render 'account/shared/box', divider: true do |box| %>
        <% box.title t('.header') %>
        <% box.description do %>
          <%= t('.description') %>
          <%= t('.manage_description') if can? :manage, @certification %>
        <% end %>

        <% box.body do %>
          <% with_attribute_settings object: @certification, strategy: :label do %>
            <%= render 'shared/attributes/belongs_to', attribute: :permit_type %>
            <%= render 'shared/attributes/date', attribute: :issued_at %>
            <%= render 'shared/attributes/date', attribute: :expires_at %>
            <%= render 'shared/attributes/text', attribute: :document_number %>
            <%= render 'shared/attributes/text', attribute: :protocol_number %>
            <%= render 'shared/attributes/date', attribute: :protocol_date %>
            <%= render 'shared/attributes/text', attribute: :training_center %>
            <%= render 'shared/attributes/date', attribute: :next_check_date %>
            <%# 🚅 super scaffolding will insert new fields above this line. %>
          <% end %>
        <% end %>

        <% box.actions do %>
          <%= link_to t('.buttons.edit'), [:edit, :account, @certification], class: first_button_primary if can? :edit, @certification %>
          <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
          <%= button_to t('.buttons.destroy'), [:account, @certification], method: :delete, class: first_button_primary, data: { turbo_confirm: t('.buttons.confirmations.destroy', model_locales(@certification)) } if can? :destroy, @certification %>
          <%= link_to t('global.buttons.back'), [:account, @worker, :certifications], class: first_button_primary %>
        <% end %>

        <% box.raw_footer do %>
          <%# 🚅 super scaffolding will insert new action model index views above this line. %>
        <% end %>
      <% end %>
    <% end %>

    <%# 🚅 super scaffolding will insert new children above this line. %>
  <% end %>
<% end %>
